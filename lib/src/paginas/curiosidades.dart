import 'package:flutter/material.dart';

class SextaPagina extends StatelessWidget {
  const SextaPagina({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Curiosidades"),
      ),
    );
  }
}

//PAGINA CURIOSIDADES