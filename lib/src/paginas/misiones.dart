import 'package:flutter/material.dart';

class CuartaPagina extends StatelessWidget {
  const CuartaPagina({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Misiones"),
      ),
    );
  }
}

//PAGINA MISIONES