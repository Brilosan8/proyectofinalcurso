import 'package:flutter/material.dart';

class QuintaPagina extends StatelessWidget {
  const QuintaPagina({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Enemigos"),
      ),
    );
  }
}

//PAGINA ENEMIGOS